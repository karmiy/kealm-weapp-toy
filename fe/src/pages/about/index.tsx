import { Text, View } from '@tarojs/components';
import './index.scss';

export default function () {
    return (
        <View className='wrapper'>
            <Text>About</Text>
        </View>
    );
}
